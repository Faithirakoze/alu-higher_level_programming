-- This script creates a new table called first_table in the current database.
-- The table has two columns: id and name.

-- This command will create a new table named first_table
CREATE TABLE IF NOT EXISTS first_table (
    id INT,
    name VARCHAR(256)
);